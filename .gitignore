# Dependencias (Nunca se suben)
node_modules/

# Archivos de configuración de paquetes (Solo ignoramos el lock si quieres, 
# aunque normalmente se recomienda subir el package-lock.json. 
# Lo que JAMÁS se sube es node_modules)
dist/
build/

# Variables de entorno (Aquí irán tus API Keys de OpenAI/Claude)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Archivos de sesión de WhatsApp (Si usas whatsapp-web.js o similar)
.wwebjs_auth/
.wwebjs_cache/
auth_info_multi/

# Sistema
.DS_Store
Thumbs.db